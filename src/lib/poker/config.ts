/** Tuning for the per-room poker table. */

/** Chips every player starts with (and rebuys to when busted). */
export const POKER_START_CHIPS = 2000;

export const POKER_SMALL_BLIND = 10;
export const POKER_BIG_BLIND = 20;

/** Max players that can be seated at one table. */
export const POKER_MAX_SEATS = 8;

/** Minimum seated players with chips needed to start a hand. */
export const POKER_MIN_PLAYERS = 2;
